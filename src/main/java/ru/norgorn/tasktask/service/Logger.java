package ru.norgorn.tasktask.service;

// Logstash for poor (never would write like this in real project)
public class Logger {

    public static void debug(String str) {
        System.out.println(str);
    }

    public static void error(String str) {
        System.out.println(str);
    }
}
